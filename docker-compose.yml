version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: service-board-mongodb
    ports:
      - '27018:27017'
    volumes:
      - mongo-data:/data/db
    restart: always

volumes:
  mongo-data:
